query PortfolioCategories {
  supports {
    edges {
      node {
        slug
        name
        id
        acfSupport {
          image {
            sourceUrl
          }
        }
      }
    }
  }
}
