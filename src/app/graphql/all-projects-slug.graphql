query projectSlugs { 
  projets(first: 1000) { 
    edges {
      node {
        slug
        supports {
          edges {
            node {
              name
              slug
            }
          }
        }
      }
    }
  }
}

