query posts { 
  posts(first: 2) { 
    edges {
      node {
        title
        slug
        date
        excerpt
        featuredImage {
          node {
            sourceUrl
          }
        }
      }
    }
  }
}
